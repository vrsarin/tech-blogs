[project]
name = "tech-blogs"
version = "0.1.0"
description = "Technical blog markdown tooling."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-frontmatter>=1.1.0",
    "sentence-transformers>=5.3.0",
]
